const concatenateArrays = (array1, array2) => {
    return array1.concat(array2)
}

console.log(concatenateArrays([1, 2], [3, 4])) // Saida: [1, 2, 3, 4]
console.log(concatenateArrays(['a', 'b'], ['c', 'd'])) // Saida: ['a', 'b', 'c', 'd']